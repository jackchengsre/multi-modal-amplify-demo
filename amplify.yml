version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build process for SRE Copilot Demo"
    build:
      commands:
        - echo "No build commands needed for static site"
    postBuild:
      commands:
        - echo "Build completed successfully"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
